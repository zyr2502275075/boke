<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>我的博客文章列表</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <!--width=device-width, initial-scale=1适当的绘制和触屏缩放;user-scalable=no禁用缩放-->
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <!--bootstrap官方样式-->
    <link rel="stylesheet" href="/css/normalize.css">
    <!--强跨浏览器表现的一致性css 重置样式-->
    <link rel="stylesheet" href="/css/main.css">
    <!--自用样式-->
</head>

<body>

<!-- 头部区域 -->
<%- include('header') %>

<!-- 导航区域 -->
<%- include('nav') %>

<!-- 主体 -->
<section class="container">

    <!-- 摘录1 -->
    <article class=" col-md-8">
        <ul class="My-excerpt">
            <% for(let i = 0; i < list.length; i++) { %>
            <li class="excerpt-minic ">
                <div class="My-excerpt-box" >
                    <div class="col-md-5">
                        <% if(list[i].img != ""){ %>
                        <img src="<%= list[i].img %>" class="img-responsive">
                        <% } %>
                    </div>
                    <div class="col-md-7">
                        <h3><a href="/article/<%= list[i]._id %>"><%= list[i].title %></a></h3>
                        <span>作者：<b><%= list[i].author %></b> • <%= list[i].create_at %></span>
                        <p style="font-size: 16px; line-height: 24px; letter-spacing:2px;"><%- list[i].contents %></p>
                    </div>
                    <div class="Articles-list-right" ><a href="/article/<%= list[i]._id %>" class="btn">阅读文章</a></div>
                </div>
                <div class="cat" >
                    <span>
                        <i class="glyphicon glyphicon-heart"></i>
                        <a href="#">阅读：<%= list[i].view %></a>
                    </span>
                </div>
            </li>
            <% } %>
        </ul>

        <!-- 分页 -->
        <%- include('pagination.ejs') %>

    </article>

    <!-- 侧边栏 -->
    <%- include('aside') %>

</section>

<!-- footer -->
<%- include('footer') %>

<script src="/js/jquery.min.js"></script><!--jquery-->
<script src="/js/bootstrap.min.js"></script><!--bootstrap官方js-->
</body>
</html>
